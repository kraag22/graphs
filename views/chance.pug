extends layout

block content
  h1 Chance to win


  div(class="col-xs-12 col-sm-10 col-sm-offset-1")
    ul(id="chance-list")

  script.
    var graph = new Graphs({})
    graph.renderChance("chance-list", !{chances}, !{chancesInPreviousSeasons})



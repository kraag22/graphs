{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run test-ci",
      "request": "launch",
      "command": "npm run test-ci",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "Run app",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}"
    }
  ]
}
